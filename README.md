# CubeTures.github.io

submodules, credential manager
--
window.addEventListener('scroll', function(){
  let value = window.scrollY;
  stars.style.left = value * .25 + 'px';
  moon.style.top = value * .25 + 'px';
});
overflow-x: hidden;
